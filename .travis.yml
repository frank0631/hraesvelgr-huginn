language: java
sudo: required

services:
  - docker
  
jdk:
  - oraclejdk8

os:
  - linux

branches:
  only:
    - master

cache:
  directories:
  - $HOME/.gradle

script:
  - gradle build docker
